import { NaArtileListPipe } from './na-artile-list.pipe';

describe('NaArtileListPipe', () => {
  it('create an instance', () => {
    const pipe = new NaArtileListPipe();
    expect(pipe).toBeTruthy();
  });
});
